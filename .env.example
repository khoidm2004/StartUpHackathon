PORT=3001
NODE_ENV=development
GROQ_API_KEY=
# Optional; default llama-3.1-8b-instant (see Groq docs for current model ids)
# GROQ_MODEL=
# Optional: extra allowed CORS origin (e.g. your deployed frontend)
FRONTEND_URL=
